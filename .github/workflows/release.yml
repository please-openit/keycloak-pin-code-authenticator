name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Build with Maven
        run: mvn -B package --file pom.xml

      - name: Extract version components
        id: version
        run: |
          TAG="${GITHUB_REF_NAME}"
          # Strip leading 'v' prefix
          VERSION="${TAG#v}"
          # Keycloak stream from the -kc.<major>.<minor> suffix: one maintenance branch per KC minor
          KC="${VERSION##*-kc.}"
          if [ "$KC" = "$VERSION" ]; then
            echo "::error::tag $TAG has no -kc.<version> suffix"
            exit 1
          fi
          KC_MINOR="$(echo "$KC" | cut -d. -f1,2)"
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "branch=release/${KC_MINOR}" >> "$GITHUB_OUTPUT"

      - name: Create maintenance branch
        run: |
          BRANCH="${{ steps.version.outputs.branch }}"
          if git ls-remote --exit-code --heads origin "$BRANCH" > /dev/null 2>&1; then
            echo "Branch $BRANCH already exists, skipping creation"
          else
            git switch -c "$BRANCH"
            git push origin "$BRANCH"
          fi

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          name: ${{ steps.version.outputs.tag }}
          generate_release_notes: true
          files: target/*.jar
